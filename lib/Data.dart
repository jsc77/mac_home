class Data {
  String url, name, material, price;
  Data(this.url, this.name, this.material, this.price);
}

class Travels {
  String desc, name, price, size, images;
  Travels(this.desc, this.name, this.price, this.size, this.images);
}
